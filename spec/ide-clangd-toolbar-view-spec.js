'use babel';

import IdeClangdToolbarView from '../lib/ide-clangd-toolbar-view';

describe('IdeClangdToolbarView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
